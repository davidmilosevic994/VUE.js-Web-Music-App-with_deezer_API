export default {
  topChartsSongs(state) {
    const topChartsSongs = state.topChartsSongs;
    // console.log("Getter Playlist:", topChartsSongs); // Add this line to log the playlist data

    return topChartsSongs;
  },
};
